# Server
NODE_ENV=development
HOST=0.0.0.0
PORT=8080

# Only allow this origin for browser clients
FRONTEND_ORIGIN=http://localhost:3000

# Protect write endpoints (recommended even in dev)
ADMIN_TOKEN=change-me

# Encrypt secrets stored in DB (Gemini/OpenAI keys). In production this is required.
# Can be a passphrase, or base64-encoded 32 bytes.
SECRETS_KEY=change-me

# Gemini (server-side only; never exposed to browser)
GEMINI_API_KEY=

# DNS core
ENABLE_DNS=true
DNS_HOST=0.0.0.0
DNS_PORT=53

# Upstream DNS for forwarded queries (recommended: Unbound)
UPSTREAM_DNS=127.0.0.1:5335

# Database
DATABASE_URL=postgres://sentinel:sentinel@localhost:5432/sentinel
