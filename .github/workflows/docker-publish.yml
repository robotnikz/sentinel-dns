name: CI/CD Pipeline

on:
  workflow_run:
    workflows: ["CI"]
    branches: ["main"]
    types: [completed]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Existing release tag (e.g. v0.9.4) to build/push images for. Leave empty to run semantic-release.'
        required: false
        default: ''

concurrency:
  group: ${{ github.workflow }}-${{ github.event.workflow_run.head_sha || github.sha }}
  # Avoid cancelling in-progress releases (a cancellation can happen after the git tag is pushed but
  # before images are built/pushed, leaving an incomplete release).
  cancel-in-progress: false

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1

jobs:
  release-and-push:
    name: Semantic Release + GHCR
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: true
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Lowercase image name
        run: |
          echo "IMAGE_NAME=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm

      - name: Semantic Release
        id: rel
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          # Manual recovery path: build/push images for an already-existing release tag.
          REQUESTED_TAG="${{ github.event.inputs.tag }}"
          if [ -n "$REQUESTED_TAG" ]; then
            echo "released=true" >> "$GITHUB_OUTPUT"
            echo "tag=$REQUESTED_TAG" >> "$GITHUB_OUTPUT"
            echo "version=${REQUESTED_TAG#v}" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          PREV_TAG="$(git describe --tags --abbrev=0 2>/dev/null || true)"
          echo "prev_tag=$PREV_TAG" >> "$GITHUB_OUTPUT"

          npx -y \
            -p semantic-release@25.0.2 \
            -p @semantic-release/commit-analyzer@13.0.1 \
            -p @semantic-release/github@12.0.2 \
            -p @semantic-release/release-notes-generator@14.1.0 \
            -p conventional-changelog-conventionalcommits@8.0.0 \
            semantic-release

          # If semantic-release created a remote tag (e.g. via GitHub API), make sure we can see it.
          git fetch --tags --force

          NEW_TAG="$(git describe --tags --abbrev=0 2>/dev/null || true)"
          if [ -n "$NEW_TAG" ] && [ "$NEW_TAG" != "$PREV_TAG" ]; then
            echo "released=true" >> "$GITHUB_OUTPUT"
            echo "tag=$NEW_TAG" >> "$GITHUB_OUTPUT"
            echo "version=${NEW_TAG#v}" >> "$GITHUB_OUTPUT"
          else
            echo "released=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout release tag
        if: steps.rel.outputs.released == 'true'
        run: git checkout "${{ steps.rel.outputs.tag }}"

      - name: Set up QEMU
        if: steps.rel.outputs.released == 'true'
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        if: steps.rel.outputs.released == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Log into registry ${{ env.REGISTRY }}
        if: steps.rel.outputs.released == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        if: steps.rel.outputs.released == 'true'
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}},value=${{ steps.rel.outputs.version }}
            type=semver,pattern={{major}}.{{minor}},value=${{ steps.rel.outputs.version }}
            type=raw,value=latest

      - name: Extract Docker metadata (keepalived)
        id: meta_keepalived
        if: steps.rel.outputs.released == 'true'
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-keepalived
          tags: |
            type=semver,pattern={{version}},value=${{ steps.rel.outputs.version }}
            type=semver,pattern={{major}}.{{minor}},value=${{ steps.rel.outputs.version }}
            type=raw,value=latest

      - name: Build and push Docker image
        if: steps.rel.outputs.released == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/single/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            SENTINEL_VERSION=${{ steps.rel.outputs.version }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Build and push Docker image (keepalived)
        if: steps.rel.outputs.released == 'true'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/keepalived/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta_keepalived.outputs.tags }}
          labels: ${{ steps.meta_keepalived.outputs.labels }}
