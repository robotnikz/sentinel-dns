# Frontend (Vite)
# In dev, the frontend uses a Vite proxy to the backend at http://localhost:8080
# so you usually don't need anything here.

# Backend (Fastify)
ADMIN_TOKEN=change-me
GEMINI_API_KEY=
SECRETS_KEY=change-me
DATABASE_URL=postgres://sentinel:sentinel@localhost:5432/sentinel
# If you run the Vite dev server, keep this at http://localhost:3000 (or Vite's port).
# If you serve the built UI from the backend (single-port), set this to http://localhost:8080.
FRONTEND_ORIGIN=http://localhost:3000
PORT=8080

# DNS core
ENABLE_DNS=true
DNS_HOST=0.0.0.0
DNS_PORT=53
UPSTREAM_DNS=127.0.0.1:5335
